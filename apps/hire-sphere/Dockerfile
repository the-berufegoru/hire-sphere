# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build hire-sphere`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t hire-sphere`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system hire-sphere && \
          adduser --system -G hire-sphere hire-sphere

COPY dist/apps/hire-sphere hire-sphere/
RUN chown -R hire-sphere:hire-sphere .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix hire-sphere --omit=dev -f install

CMD [ "node", "hire-sphere" ]
